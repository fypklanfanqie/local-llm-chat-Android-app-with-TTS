<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 音频相关 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 角色问候：主动消息通知（Android 13+ 需运行时申请 POST_NOTIFICATIONS）-->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 本地推理保活：前台服务（生成期间常驻通知栏，防国产 ROM 杀进程）+ WakeLock -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 后台保活引导：电池白名单 / 精确闹钟（问候与群聊兜底）/ 开机重排闹钟 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Android 11+ 包可见性：朗读（系统 TTS）需要声明 TTS 服务查询，
         否则无法绑定系统语音引擎（小米 HyperOS 会把未声明的绑定 BLOCKED，导致 onInit 失败）。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <!-- Android 11+ 包可见性：厂商「自启动/后台管理」设置页跳转（BackgroundSurvivalHelper）。
         未声明的包 resolveActivity 恒 null -> 设置页自启动引导入口在 OPPO/vivo 等机型上静默消失。 -->
    <queries>
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.hihonor.systemmanager" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.meizu.safe" />
        <package android:name="com.samsung.android.lool" />
    </queries>

    <application
        android:name=".ChatApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatByYourSide"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- OpenCL 探测（Task 10）：声明可选 vendor OpenCL 库；探测进程动态加载，缺库不崩溃。 -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.ChatByYourSide">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OpenCL 执行探测服务（Task 10）：独立进程，驱动崩溃不波及主进程。 -->
        <service
            android:name=".llm.backend.OpenClProbeService"
            android:process=":mnn_probe"
            android:exported="false" />

        <!-- 本地推理保活前台服务（生成期间常驻通知栏 + WakeLock，防国产 ROM 杀进程） -->
        <service
            android:name=".service.InferenceForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- WorkManager 前台 Worker（GreetingWorker/GroupChatWorker 生成时 setForeground 用），与自定义服务共存需 merge -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- 问候精确闹钟兜底：WorkManager 周期工作被国产 ROM 冻结时，闹钟到点触发一次问候 Worker -->
        <receiver
            android:name=".work.GreetingAlarmReceiver"
            android:exported="false" />

        <!-- 群聊精确闹钟兜底：闹钟到点触发一次群聊 Worker（空闲自动聊天） -->
        <receiver
            android:name=".work.GroupChatAlarmReceiver"
            android:exported="false" />

        <!-- 开机重排问候/群聊闹钟（WorkManager 周期工作开机自动恢复，仅精确闹钟需手动重排） -->
        <receiver
            android:name=".work.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
